# language setting
language: ruby

rvm:
  - 1.9.2
  - 2.2.4
services:
    - postgres

build:
  pre_ci_boot:
    image_name: drydock/u14rubpls
    image_tag: prod
    pull: true
    options: --privileged=true
  ci:    
    - mkdir -p ~/.aws   
    - bundle install
